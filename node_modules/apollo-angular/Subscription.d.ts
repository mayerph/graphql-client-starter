import { DocumentNode } from 'graphql';
import { Observable } from 'rxjs';
import { Apollo } from './Apollo';
import { SubscriptionOptions, ExtraSubscriptionOptions, SubscriptionResult, R } from './types';
export declare class Subscription<T = any, V = R> {
    protected apollo: Apollo;
    readonly document: DocumentNode;
    client: string;
    constructor(apollo: Apollo);
    subscribe(variables?: V, options?: SubscriptionOptions<V>, extra?: ExtraSubscriptionOptions): Observable<SubscriptionResult<T>>;
}
